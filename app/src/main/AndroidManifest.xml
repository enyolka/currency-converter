<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emilia_maczka_czw_9_30">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Emilia_Maczka_czw_9_30">
        <activity
            android:name=".ExchangeActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".GoldActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CurrenciesListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".HistoricRatesActivity"
            android:parentActivityName=".CurrenciesListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>